import { CutSearchDirective } from './cut-search.directive';

describe('CutSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new CutSearchDirective();
    expect(directive).toBeTruthy();
  });
});
